{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Default-Build-Test",
            "type": "shell",
            "command": "g++ -ggdb -std=c++11 -O0 -o BuildTest.bin Main.cpp Sort.cpp Timer.cpp",
            //"command": "g++ -ggdb -O0 -o BuidTest.bin Put_cpp_file_here",
            // "command": "g++ -ggdb -O0 -Wall -o BuidTest.bin Put_cpp_file_here", // Includes -Wall which gives more (all) warning prompts.
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}